thorntail:
  datasources:
    data-sources:
      ThorntailDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://localhost:5432/thorntail_liquibase
        user-name: postgres
        password: postgres
