thorntail:
  datasources:
    data-sources:
      ThorntailDS:
        driver-name: postgresql
        connection-url: jdbc:postgresql://localhost:5432/enterprise
        user-name: postgres
        password: postgres
